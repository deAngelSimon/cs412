// Function to cube the value
const cubed = (x) => {
    return console.log(`${x*x*x}`)
}
// The values
let values = [1,2, 3, 4, 5, 6, 7];

// A single line of code that call the function on all values

values.map(cubed);


